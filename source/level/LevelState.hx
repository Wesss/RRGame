package level;

/**
 * Represents the states of a level from a perspecive the top-most module
 **/
enum LevelState {
    // TODO remove all of this junk, create additional busses if needed
    LOAD;
    START;
    WIN;
    LOSE;
}